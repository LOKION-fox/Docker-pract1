FROM nginx:alpine

LABEL version="1.0.1"

# копируем все html-файлы из текущей папки в стандартную папку Nginx
COPY . /usr/share/nginx/html

# инструкция указывающая на то, какой порт используется (обычно 80); может быть "информационной"
EXPOSE 80